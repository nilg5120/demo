CREATE TABLE jp_combos (
    id INT  AUTO_INCREMENT PRIMARY KEY,
    damage VARCHAR(255),
    `explain` TEXT,
    hittype VARCHAR(255),
    input VARCHAR(255),
    name VARCHAR(255),
    startup INT,
    usagedg INT,
    usagesa INT
);
